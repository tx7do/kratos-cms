// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable no-use-before-define, @typescript-eslint/no-unused-vars, prettier/prettier */
// @ts-nocheck: generated code does not require type checking

interface TransportMeta {
  service: string;
  method: string;
}

export interface ClientTransport {
  unary(
    path: string,
    method: string,
    body: null | string,
    meta: TransportMeta,
  ): Promise<unknown>;
  serverStream<T>(path: string, meta: TransportMeta): ServerStream<T>;
  duplexStream<TIn, TOut>(path: string, meta: TransportMeta): DuplexStream<TIn, TOut>;
}

export interface ServerStream<T> {
  onEvent(listener: (data: T) => void): () => void;
  onError(handler: (error: Error) => void): void;
  close(): void;
}

export interface DuplexStream<TIn, TOut> extends ServerStream<TOut> {
  send(data: TIn): void;
}

// 前台应用错误定义
export type AppErrorReason =
  // 502
  | 'BAD_GATEWAY'
  // 400
  | 'BAD_REQUEST'
  // 409
  | 'CONFLICT'
  // 417
  | 'EXPECTATION_FAILED'
  // 424
  | 'FAILED_DEPENDENCY'
  // 403
  | 'FORBIDDEN'
  // 504
  | 'GATEWAY_TIMEOUT'
  // 410
  | 'GONE'
  // 505
  | 'HTTP_VERSION_NOT_SUPPORTED'
  // 418
  | 'IM_A_TEAPOT'
  | 'INCORRECT_ACCESS_TOKEN'
  | 'INCORRECT_APP_SECRET'
  | 'INCORRECT_REFRESH_TOKEN'
  // 507
  | 'INSUFFICIENT_STORAGE'
  // 500
  | 'INTERNAL_SERVER_ERROR'
  | 'INVALID_GRANT_TYPE'
  | 'INVALID_PASSWORD'
  | 'INVALID_TOKEN'
  | 'INVALID_USERID'
  // 411
  | 'LENGTH_REQUIRED'
  // 423
  | 'LOCKED'
  // 508
  | 'LOOP_DETECTED'
  // 405
  | 'METHOD_NOT_ALLOWED'
  // 421
  | 'MISDIRECTED_REQUEST'
  // 511
  | 'NETWORK_AUTHENTICATION_REQUIRED'
  // 599
  | 'NETWORK_CONNECT_TIMEOUT_ERROR'
  // 598
  | 'NETWORK_READ_TIMEOUT_ERROR'
  // 406
  | 'NOT_ACCEPTABLE'
  // 510
  | 'NOT_EXTENDED'
  // 404
  | 'NOT_FOUND'
  // 501
  | 'NOT_IMPLEMENTED'
  // 413
  | 'PAYLOAD_TOO_LARGE'
  // 402
  | 'PAYMENT_REQUIRED'
  // 412
  | 'PRECONDITION_FAILED'
  // 428
  | 'PRECONDITION_REQUIRED'
  // 407
  | 'PROXY_AUTHENTICATION_REQUIRED'
  // 416
  | 'RANGE_NOT_SATISFIABLE'
  // 431
  | 'REQUEST_HEADER_FIELDS_TOO_LARGE'
  // 408
  | 'REQUEST_TIMEOUT'
  // 503
  | 'SERVICE_UNAVAILABLE'
  | 'TOKEN_EXPIRED'
  | 'TOKEN_NOT_EXIST'
  // 425
  | 'TOO_EARLY'
  // 429
  | 'TOO_MANY_REQUESTS'
  // 401
  | 'UNAUTHORIZED'
  // 451
  | 'UNAVAILABLE_FOR_LEGAL_REASONS'
  // 422
  | 'UNPROCESSABLE_ENTITY'
  // 415
  | 'UNSUPPORTED_MEDIA_TYPE'
  // 426
  | 'UPGRADE_REQUIRED'
  // 414
  | 'URI_TOO_LONG'
  | 'USER_FREEZE'
  | 'USER_NOT_FOUND'
  // 506
  | 'VARIANT_ALSO_NEGOTIATES';
// 用户前台登录认证服务
export interface AuthenticationService {
  // 登录
  Login(
    request: authenticationservicev1_LoginRequest,
  ): Promise<authenticationservicev1_LoginResponse>;
  // 登出
  Logout(
    request: wellKnownEmpty,
  ): Promise<wellKnownEmpty>;
  // 刷新认证令牌
  RefreshToken(
    request: authenticationservicev1_LoginRequest,
  ): Promise<authenticationservicev1_LoginResponse>;
}

export function createAuthenticationServiceClient(
  transport: ClientTransport,
): AuthenticationService {
  return {
    Login(request) {
      const path = `app/v1/login`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'AuthenticationService',
        method: 'Login',
      }) as Promise<authenticationservicev1_LoginResponse>;
    },
    Logout(request) {
      const path = `app/v1/logout`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'AuthenticationService',
        method: 'Logout',
      }) as Promise<wellKnownEmpty>;
    },
    RefreshToken(request) {
      const path = `app/v1/refresh-token`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'AuthenticationService',
        method: 'RefreshToken',
      }) as Promise<authenticationservicev1_LoginResponse>;
    },
  };
}
// 用户登录 - 请求
export type authenticationservicev1_LoginRequest = {
  client_id?: string;
  client_secret?: string;
  client_type?: authenticationservicev1_ClientType;
  code?: string;
  device_id?: string;
  email?: string;
  //
  // Behaviors: REQUIRED
  grant_type: authenticationservicev1_GrantType | undefined;
  jti?: string;
  mobile?: string;
  password?: string;
  redirect_uri?: string;
  refresh_token?: string;
  scope?: string;
  tenant_code?: string;
  user_id?: number;
  username?: string;
};

// 授权类型
export type authenticationservicev1_GrantType =
  | 'authorization_code'
  | 'client_credentials'
  | 'implicit'
  | 'password'
  | 'refresh_token';
// 客户端类型
export type authenticationservicev1_ClientType =
  | 'admin'
  | 'app';
// 用户登录 - 回应
export type authenticationservicev1_LoginResponse = {
  access_token: string | undefined;
  expires_in: number | undefined;
  id_token?: string;
  refresh_expires_in?: number;
  refresh_token?: string;
  scope?: string;
  token_type: authenticationservicev1_TokenType | undefined;
};

// 令牌类型
export type authenticationservicev1_TokenType =
  | 'bearer'
  | 'mac';
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// 类别服务
export interface CategoryService {
  // 获取类别列表
  List(
    request: pagination_PagingRequest,
  ): Promise<contentservicev1_ListCategoryResponse>;
  // 获取类别数据
  Get(
    request: contentservicev1_GetCategoryRequest,
  ): Promise<contentservicev1_Category>;
  // 创建类别
  Create(
    request: contentservicev1_CreateCategoryRequest,
  ): Promise<contentservicev1_Category>;
  // 更新类别
  Update(
    request: contentservicev1_UpdateCategoryRequest,
  ): Promise<contentservicev1_Category>;
  // 删除类别
  Delete(
    request: contentservicev1_DeleteCategoryRequest,
  ): Promise<wellKnownEmpty>;
  // 获取翻译数据
  GetTranslation(
    request: contentservicev1_GetCategoryRequest,
  ): Promise<contentservicev1_CategoryTranslation>;
}

export function createCategoryServiceClient(
  transport: ClientTransport,
): CategoryService {
  return {
    List(request) {
      const path = `app/v1/categories`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'CategoryService',
        method: 'List',
      }) as Promise<contentservicev1_ListCategoryResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/categories/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'CategoryService',
        method: 'Get',
      }) as Promise<contentservicev1_Category>;
    },
    Create(request) {
      const path = `app/v1/categories`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'CategoryService',
        method: 'Create',
      }) as Promise<contentservicev1_Category>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/categories/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'CategoryService',
        method: 'Update',
      }) as Promise<contentservicev1_Category>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/categories/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'CategoryService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
    GetTranslation(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/categories/${request.id}/translation`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'CategoryService',
        method: 'GetTranslation',
      }) as Promise<contentservicev1_CategoryTranslation>;
    },
  };
}
// ------------------------------
// 分页通用请求
// ------------------------------
export type pagination_PagingRequest = {
  // 字段掩码，其作用为SELECT中的字段，其语法为使用逗号分隔字段名，例如：id,realName,userName。如果为空则选中所有字段，即SELECT *。
  fieldMask?: wellKnownFieldMask;
  // Google AIP规范字符串过滤条件
  filter?: string;
  // 复杂过滤表达式（优先使用）
  filterExpr?: pagination_FilterExpr;
  // 最多返回的记录数（默认10，建议设置上限如100）
  limit?: number;
  // 是否不分页，如果为true，则page和pageSize参数无效。
  noPaging?: boolean;
  // 跳过的记录数（从0开始，默认0）
  offset?: number;
  // 排序条件
  orderBy?: string;
  // 当前页码（从1开始，默认1）
  page?: number;
  // 每页条数（默认10，建议设置上限如100）
  pageSize?: number;
  // JSON字符串过滤条件，基础语法：{"field1":"val1", "field2___icontains":"val2"}，具体请参见：https://github.com/tx7do/go-crud/tree/main/pagination/filter/README.md
  query?: string;
  // 排序规则
  sorting: pagination_Sorting[] | undefined;
  // 上一页最后一条记录的游标（如ID/时间戳+ID，首次请求为空）
  token?: string;
};

// 过滤表达式
export type pagination_FilterExpr = {
  // 条件列表
  conditions: pagination_FilterCondition[] | undefined;
  // 子表达式列表
  groups: pagination_FilterExpr[] | undefined;
  // 过滤表达式类型
  type: pagination_ExprType | undefined;
};

// 过滤表达式类型
export type pagination_ExprType =
  | 'AND'
  | 'EXPR_TYPE_UNSPECIFIED'
  | 'OR';
// 过滤条件
export type pagination_FilterCondition = {
  // 日期时间部分（可选，仅在字段为日期时间类型时使用）
  datePart?: pagination_DatePart;
  // 过滤字段名
  field: string | undefined;
  // 当字段为 JSON/JSONB 类型时，可指定要抽取的子路径（例如: "meta.user.name" 或 JSONPath）
  // 服务端应把此路径用于 JSON_EXTRACT / -> 操作，再对抽取结果应用 op。
  jsonPath?: string;
  // 当需要使用非字符串类型的比较值（对象/数组/数字/布尔）时使用此字段，
  // 使用 google.protobuf.Value 能表达任意 JSON 值。
  jsonValue?: wellKnownValue;
  // 过滤操作符
  op: pagination_Operator | undefined;
  // 过滤值（单值）
  value?: string;
  // 过滤值（多值，如IN操作符）
  values: string[] | undefined;
};

// 操作符枚举
export type pagination_Operator =
  | 'ARRAY_CONTAINS'
  // 范围与正则
  | 'BETWEEN'
  // 语义化的字符串操作
  | 'CONTAINS'
  | 'ENDS_WITH'
  // 基本比较
  | 'EQ'
  | 'EXACT'
  | 'EXISTS'
  | 'GT'
  | 'GTE'
  | 'ICONTAINS'
  | 'IENDS_WITH'
  | 'IEXACT'
  | 'ILIKE'
  // 集合操作
  | 'IN'
  | 'IREGEXP'
  | 'IS_NOT_NULL'
  // 空值判断
  | 'IS_NULL'
  | 'ISTARTS_WITH'
  // JSON / 数组 / 集合相关（按需在服务端映射为具体 DB 运算）
  | 'JSON_CONTAINS'
  // 模糊 / 大小写不敏感模糊 / 非模糊
  | 'LIKE'
  | 'LT'
  | 'LTE'
  | 'NEQ'
  | 'NIN'
  | 'NOT_LIKE'
  // 未指定
  | 'OPERATOR_UNSPECIFIED'
  | 'REGEXP'
  | 'SEARCH'
  | 'STARTS_WITH';
type wellKnownValue = unknown;

// 日期时间部分枚举
export type pagination_DatePart =
  | 'DATE'
  | 'DATE_PART_UNSPECIFIED'
  | 'DAY'
  | 'HOUR'
  | 'ISO_WEEK_DAY'
  | 'ISO_YEAR'
  | 'MICROSECOND'
  | 'MINUTE'
  | 'MONTH'
  | 'QUARTER'
  | 'SECOND'
  | 'TIME'
  | 'WEEK'
  | 'WEEK_DAY'
  | 'YEAR';
// 排序规则（分页场景通常需配合排序保证结果稳定）
export type pagination_Sorting = {
  // 排序方向
  direction: pagination_Sorting_Direction | undefined;
  // 排序字段（如"id"、"create_time"）
  field: string | undefined;
};

// 排序方向（ASC/DESC，默认ASC）
export type pagination_Sorting_Direction =
  | 'ASC'
  | 'DESC';
// In JSON, a field mask is encoded as a single string where paths are
// separated by a comma. Fields name in each path are converted
// to/from lower-camel naming conventions.
// As an example, consider the following message declarations:
//
//     message Profile {
//       User user = 1;
//       Photo photo = 2;
//     }
//     message User {
//       string display_name = 1;
//       string address = 2;
//     }
//
// In proto a field mask for `Profile` may look as such:
//
//     mask {
//       paths: "user.display_name"
//       paths: "photo"
//     }
//
// In JSON, the same mask is represented as below:
//
//     {
//       mask: "user.displayName,photo"
//     }
type wellKnownFieldMask = string;

// 回应 - 类别列表
export type contentservicev1_ListCategoryResponse = {
  items: contentservicev1_Category[] | undefined;
  total: number | undefined;
};

// 类别
export type contentservicev1_Category = {
  availableLanguages: string[] | undefined;
  children: contentservicev1_Category[] | undefined;
  code?: string;
  contentModelId?: number;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  customFields: { [key: string]: string } | undefined;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  depth?: number;
  directPostCount?: number;
  icon?: string;
  id?: number;
  isNav?: boolean;
  parentId?: number;
  path?: string;
  postCount?: number;
  sortOrder?: number;
  status?: contentservicev1_Category_CategoryStatus;
  thumbnail?: string;
  translations: contentservicev1_CategoryTranslation[] | undefined;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 分类状态
export type contentservicev1_Category_CategoryStatus =
  | 'CATEGORY_STATUS_ACTIVE'
  | 'CATEGORY_STATUS_ARCHIVED'
  | 'CATEGORY_STATUS_HIDDEN'
  | 'CATEGORY_STATUS_UNSPECIFIED';
// 类别翻译
export type contentservicev1_CategoryTranslation = {
  categoryId?: number;
  coverImage?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  description?: string;
  fullPath?: string;
  id?: number;
  languageCode?: string;
  name?: string;
  seo?: contentservicev1_SeoMeta;
  slug?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// SEO 元信息结构设计
export type contentservicev1_SeoMeta = {
  canonicalUrl?: string;
  metaDescription?: string;
  metaKeywords?: string;
  ogDescription?: string;
  ogImage?: string;
  ogTitle?: string;
  seoTitle?: string;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

// 请求 - 类别数据
export type contentservicev1_GetCategoryRequest = {
  code?: string;
  id?: number;
  locale?: string;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建类别
export type contentservicev1_CreateCategoryRequest = {
  data: contentservicev1_Category | undefined;
};

// 请求 - 更新类别
export type contentservicev1_UpdateCategoryRequest = {
  allowMissing?: boolean;
  data: contentservicev1_Category | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除类别
export type contentservicev1_DeleteCategoryRequest = {
  id?: number;
};

// 评论服务
export interface CommentService {
  // 获取评论列表
  List(
    request: pagination_PagingRequest,
  ): Promise<commentservicev1_ListCommentResponse>;
  // 获取评论数据
  Get(
    request: commentservicev1_GetCommentRequest,
  ): Promise<commentservicev1_Comment>;
  // 创建评论
  Create(
    request: commentservicev1_CreateCommentRequest,
  ): Promise<commentservicev1_Comment>;
  // 更新评论
  Update(
    request: commentservicev1_UpdateCommentRequest,
  ): Promise<commentservicev1_Comment>;
  // 删除评论
  Delete(
    request: commentservicev1_DeleteCommentRequest,
  ): Promise<wellKnownEmpty>;
}

export function createCommentServiceClient(
  transport: ClientTransport,
): CommentService {
  return {
    List(request) {
      const path = `app/v1/comments`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'CommentService',
        method: 'List',
      }) as Promise<commentservicev1_ListCommentResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/comments/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'CommentService',
        method: 'Get',
      }) as Promise<commentservicev1_Comment>;
    },
    Create(request) {
      const path = `app/v1/comments`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'CommentService',
        method: 'Create',
      }) as Promise<commentservicev1_Comment>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/comments/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'CommentService',
        method: 'Update',
      }) as Promise<commentservicev1_Comment>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/comments/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'CommentService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// 回应 - 评论列表
export type commentservicev1_ListCommentResponse = {
  items: commentservicev1_Comment[] | undefined;
  total: number | undefined;
};

// 评论
export type commentservicev1_Comment = {
  authorEmail?: string;
  authorId?: number;
  authorName?: string;
  authorType?: commentservicev1_Comment_AuthorType;
  authorUrl?: string;
  children: commentservicev1_Comment[] | undefined;
  content?: string;
  contentType?: commentservicev1_Comment_ContentType;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  detectedLanguage?: string;
  id?: number;
  ipAddress?: string;
  isSpam?: boolean;
  isSticky?: boolean;
  location?: string;
  objectId?: number;
  parentId?: number;
  replyToId?: number;
  status?: commentservicev1_Comment_Status;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
  userAgent?: string;
};

// 内容类型
export type commentservicev1_Comment_ContentType =
  | 'CONTENT_TYPE_PAGE'
  | 'CONTENT_TYPE_POST'
  | 'CONTENT_TYPE_PRODUCT'
  | 'CONTENT_TYPE_UNSPECIFIED';
// 作者类型
export type commentservicev1_Comment_AuthorType =
  | 'AUTHOR_TYPE_ADMIN'
  | 'AUTHOR_TYPE_GUEST'
  | 'AUTHOR_TYPE_MODERATOR'
  | 'AUTHOR_TYPE_UNSPECIFIED'
  | 'AUTHOR_TYPE_USER';
// 评论状态
export type commentservicev1_Comment_Status =
  | 'STATUS_APPROVED'
  | 'STATUS_PENDING'
  | 'STATUS_REJECTED'
  | 'STATUS_SPAM'
  | 'STATUS_UNSPECIFIED';
// 请求 - 评论数据
export type commentservicev1_GetCommentRequest = {
  id: number | undefined;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建评论
export type commentservicev1_CreateCommentRequest = {
  data: commentservicev1_Comment | undefined;
};

// 请求 - 更新评论
export type commentservicev1_UpdateCommentRequest = {
  allowMissing?: boolean;
  data: commentservicev1_Comment | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除评论
export type commentservicev1_DeleteCommentRequest = {
  id?: number;
};

// 文件传输服务
export interface FileTransferService {
  // 下载文件
  DownloadFile(
    request: storageservicev1_DownloadFileRequest,
  ): Promise<storageservicev1_DownloadFileResponse>;
  // 上传文件 PUT 方式
  PutUploadFile(
    request: storageservicev1_UploadFileRequest,
  ): Promise<storageservicev1_UploadFileResponse>;
  // 上传文件 POST 方式
  PostUploadFile(
    request: storageservicev1_UploadFileRequest,
  ): Promise<storageservicev1_UploadFileResponse>;
}

export function createFileTransferServiceClient(
  transport: ClientTransport,
): FileTransferService {
  return {
    DownloadFile(request) {
      const path = `app/v1/file/download`;
      const body = null;
      const queryParams: string[] = [];
      if (request.fileId) {
        queryParams.push(
          `fileId=${encodeURIComponent(request.fileId.toString())}`,
        );
      }
      if (request.storageObject?.bucketName) {
        queryParams.push(
          `storageObject.bucketName=${encodeURIComponent(request.storageObject.bucketName.toString())}`,
        );
      }
      if (request.storageObject?.fileDirectory) {
        queryParams.push(
          `storageObject.fileDirectory=${encodeURIComponent(request.storageObject.fileDirectory.toString())}`,
        );
      }
      if (request.storageObject?.objectName) {
        queryParams.push(
          `storageObject.objectName=${encodeURIComponent(request.storageObject.objectName.toString())}`,
        );
      }
      if (request.downloadUrl) {
        queryParams.push(
          `downloadUrl=${encodeURIComponent(request.downloadUrl.toString())}`,
        );
      }
      if (request.rangeStart) {
        queryParams.push(
          `rangeStart=${encodeURIComponent(request.rangeStart.toString())}`,
        );
      }
      if (request.rangeEnd) {
        queryParams.push(
          `rangeEnd=${encodeURIComponent(request.rangeEnd.toString())}`,
        );
      }
      if (request.preferPresignedUrl) {
        queryParams.push(
          `preferPresignedUrl=${encodeURIComponent(request.preferPresignedUrl.toString())}`,
        );
      }
      if (request.presignExpireSeconds) {
        queryParams.push(
          `presignExpireSeconds=${encodeURIComponent(request.presignExpireSeconds.toString())}`,
        );
      }
      if (request.disposition) {
        queryParams.push(
          `disposition=${encodeURIComponent(request.disposition.toString())}`,
        );
      }
      if (request.acceptMime) {
        queryParams.push(
          `acceptMime=${encodeURIComponent(request.acceptMime.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'FileTransferService',
        method: 'DownloadFile',
      }) as Promise<storageservicev1_DownloadFileResponse>;
    },
    PutUploadFile(request) {
      const path = `app/v1/file/upload`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'FileTransferService',
        method: 'PutUploadFile',
      }) as Promise<storageservicev1_UploadFileResponse>;
    },
    PostUploadFile(request) {
      const path = `app/v1/file/upload`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'FileTransferService',
        method: 'PostUploadFile',
      }) as Promise<storageservicev1_UploadFileResponse>;
    },
  };
}
// 文件下载请求
export type storageservicev1_DownloadFileRequest = {
  acceptMime?: string;
  disposition?: string;
  downloadUrl?: string;
  fileId?: number;
  preferPresignedUrl?: boolean;
  presignExpireSeconds?: number;
  rangeEnd?: number;
  // 可选的分段下载范围（闭区间 start，开区间 end；为 0/0 表示全量）
  rangeStart?: number;
  storageObject?: storageservicev1_StorageObject;
};

// 对象存储对象
export type storageservicev1_StorageObject = {
  bucketName?: string;
  fileDirectory?: string;
  objectName?: string;
};

// 文件下载响应
export type storageservicev1_DownloadFileResponse = {
  checksum: string | undefined;
  downloadUrl?: string;
  file?: string;
  mime: string | undefined;
  size: number | undefined;
  sourceFileName: string | undefined;
  storagePath: string | undefined;
  updatedAt?: wellKnownTimestamp;
};

export type storageservicev1_UploadFileRequest = {
  file?: string;
  mime?: string;
  presign?: storageservicev1_PresignOption;
  size?: number;
  sourceFileName?: string;
  storageObject: storageservicev1_StorageObject | undefined;
  tenantId?: number;
  userId?: number;
};

// 预签名选项
export type storageservicev1_PresignOption = {
  contentType?: string;
  expireSeconds?: number;
  method?: string;
};

export type storageservicev1_UploadFileResponse = {
  objectName?: string;
  presignedUrl?: string;
};

// 交互服务（点赞/收藏）
export interface InteractionService {
  // 点赞
  Like(
    request: interactionservicev1_LikeRequest,
  ): Promise<interactionservicev1_LikeResponse>;
  // 取消点赞
  Unlike(
    request: interactionservicev1_LikeRequest,
  ): Promise<interactionservicev1_LikeResponse>;
  // 收藏 post
  Watch(
    request: interactionservicev1_WatchRequest,
  ): Promise<interactionservicev1_WatchResponse>;
  // 取消收藏 post
  Unwatch(
    request: interactionservicev1_WatchRequest,
  ): Promise<interactionservicev1_WatchResponse>;
  // 批量查询交互状态
  GetInteractionStatus(
    request: interactionservicev1_GetInteractionStatusRequest,
  ): Promise<interactionservicev1_GetInteractionStatusResponse>;
  // 列出当前 viewer 收藏的 post
  ListWatchedPosts(
    request: pagination_PagingRequest,
  ): Promise<contentservicev1_ListPostResponse>;
  // 批量查询计数
  GetCounts(
    request: interactionservicev1_GetCountsRequest,
  ): Promise<interactionservicev1_GetCountsResponse>;
}

export function createInteractionServiceClient(
  transport: ClientTransport,
): InteractionService {
  return {
    Like(request) {
      const path = `app/v1/interactions/like`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'Like',
      }) as Promise<interactionservicev1_LikeResponse>;
    },
    Unlike(request) {
      const path = `app/v1/interactions/unlike`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'Unlike',
      }) as Promise<interactionservicev1_LikeResponse>;
    },
    Watch(request) {
      const path = `app/v1/interactions/watch`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'Watch',
      }) as Promise<interactionservicev1_WatchResponse>;
    },
    Unwatch(request) {
      const path = `app/v1/interactions/unwatch`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'Unwatch',
      }) as Promise<interactionservicev1_WatchResponse>;
    },
    GetInteractionStatus(request) {
      const path = `app/v1/interactions/status`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'GetInteractionStatus',
      }) as Promise<interactionservicev1_GetInteractionStatusResponse>;
    },
    ListWatchedPosts(request) {
      const path = `app/v1/interactions/watched-posts`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'InteractionService',
        method: 'ListWatchedPosts',
      }) as Promise<contentservicev1_ListPostResponse>;
    },
    GetCounts(request) {
      const path = `app/v1/interactions/counts:list`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'InteractionService',
        method: 'GetCounts',
      }) as Promise<interactionservicev1_GetCountsResponse>;
    },
  };
}
export type interactionservicev1_LikeRequest = {
  targetId: number | undefined;
  targetType: interactionservicev1_TargetType | undefined;
};

// 交互目标类型
export type interactionservicev1_TargetType =
  | 'TARGET_TYPE_COMMENT'
  | 'TARGET_TYPE_POST'
  | 'TARGET_TYPE_UNSPECIFIED';
export type interactionservicev1_LikeResponse = {
  likeCount: number | undefined;
  liked: boolean | undefined;
};

export type interactionservicev1_WatchRequest = {
  postId: number | undefined;
};

export type interactionservicev1_WatchResponse = {
  watchCount: number | undefined;
  watched: boolean | undefined;
};

export type interactionservicev1_GetInteractionStatusRequest = {
  targetIds: number[] | undefined;
  targetType: interactionservicev1_TargetType | undefined;
};

export type interactionservicev1_GetInteractionStatusResponse = {
  statuses: { [key: string]: interactionservicev1_InteractionStatus } | undefined;
};

export type interactionservicev1_InteractionStatus = {
  liked: boolean | undefined;
  watched: boolean | undefined;
};

// 回应 - 帖子列表
export type contentservicev1_ListPostResponse = {
  items: contentservicev1_Post[] | undefined;
  total: number | undefined;
};

// 帖子
export type contentservicev1_Post = {
  authorId?: number;
  authorName?: string;
  autoSummary?: boolean;
  availableLanguages: string[] | undefined;
  categoryIds: number[] | undefined;
  code?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  customFields: { [key: string]: string } | undefined;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  disallowComment?: boolean;
  editorType?: contentservicev1_EditorType;
  id?: number;
  inProgress?: boolean;
  isFeatured?: boolean;
  passwordHash?: string;
  publishTime?: wellKnownTimestamp;
  sortOrder?: number;
  status?: contentservicev1_Post_PostStatus;
  tagIds: number[] | undefined;
  thumbnail?: string;
  translations: contentservicev1_PostTranslation[] | undefined;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 帖子状态
export type contentservicev1_Post_PostStatus =
  | 'POST_STATUS_DRAFT'
  | 'POST_STATUS_PUBLISHED'
  | 'POST_STATUS_SCHEDULED'
  | 'POST_STATUS_TRASHED'
  | 'POST_STATUS_UNSPECIFIED';
// 编辑器类型
export type contentservicev1_EditorType =
  | 'EDITOR_TYPE_CODE'
  // 扩展类型（预留，默认不展示，供高级功能扩展）
  | 'EDITOR_TYPE_JSON_BLOCK'
  | 'EDITOR_TYPE_MARKDOWN'
  | 'EDITOR_TYPE_PLAIN_TEXT'
  | 'EDITOR_TYPE_RICH_TEXT'
  | 'EDITOR_TYPE_UNSPECIFIED'
  | 'EDITOR_TYPE_VISUAL_BUILDER';
// 帖子翻译
export type contentservicev1_PostTranslation = {
  content?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  fullPath?: string;
  id?: number;
  languageCode?: string;
  originalContent?: string;
  postId?: number;
  seo?: contentservicev1_SeoMeta;
  slug?: string;
  summary?: string;
  title?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
  wordCount?: number;
};

export type interactionservicev1_GetCountsRequest = {
  metrics: interactionservicev1_CounterMetric[] | undefined;
  targetIds: number[] | undefined;
  targetType: interactionservicev1_TargetType | undefined;
};

// 计数指标类型。与 interaction_counter 表的 metric 列对应。
// 当前仅点赞计数活跃；后续若新增浏览量/点踩等，在此扩展枚举值即可，无需改 schema。
export type interactionservicev1_CounterMetric =
  | 'COUNTER_METRIC_LIKE'
  | 'COUNTER_METRIC_UNSPECIFIED'
  | 'COUNTER_METRIC_WATCH';
// target_id → 该目标下各 metric 的计数集合。
// 未在 counter 表中记录的 (target, metric) 组合不出现在响应中，前端按 0 兜底。
export type interactionservicev1_GetCountsResponse = {
  counts: { [key: string]: interactionservicev1_CountMap } | undefined;
};

// 单个目标下所有请求 metric 的计数集合。
export type interactionservicev1_CountMap = {
  counts: interactionservicev1_MetricCount[] | undefined;
};

// 单个 (target, metric) 的计数条目。用于 GetCountsResponse 的内层结构。
// 注意：proto3 不允许枚举类型作 map key，故内层用 repeated MetricCount 而非 map<CounterMetric,int64>。
export type interactionservicev1_MetricCount = {
  count: number | undefined;
  metric: interactionservicev1_CounterMetric | undefined;
};

// 导航服务
export interface NavigationService {
  // 获取导航列表
  List(
    request: pagination_PagingRequest,
  ): Promise<siteservicev1_ListNavigationResponse>;
  // 获取导航数据
  Get(
    request: siteservicev1_GetNavigationRequest,
  ): Promise<siteservicev1_Navigation>;
  // 创建导航
  Create(
    request: siteservicev1_CreateNavigationRequest,
  ): Promise<siteservicev1_Navigation>;
  // 更新导航
  Update(
    request: siteservicev1_UpdateNavigationRequest,
  ): Promise<siteservicev1_Navigation>;
  // 删除导航
  Delete(
    request: siteservicev1_DeleteNavigationRequest,
  ): Promise<wellKnownEmpty>;
}

export function createNavigationServiceClient(
  transport: ClientTransport,
): NavigationService {
  return {
    List(request) {
      const path = `app/v1/navigations`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'NavigationService',
        method: 'List',
      }) as Promise<siteservicev1_ListNavigationResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/navigations/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.name) {
        queryParams.push(
          `name=${encodeURIComponent(request.name.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'NavigationService',
        method: 'Get',
      }) as Promise<siteservicev1_Navigation>;
    },
    Create(request) {
      const path = `app/v1/navigations`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'NavigationService',
        method: 'Create',
      }) as Promise<siteservicev1_Navigation>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/navigations/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'NavigationService',
        method: 'Update',
      }) as Promise<siteservicev1_Navigation>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/navigations/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'NavigationService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// 回应 - 导航列表
export type siteservicev1_ListNavigationResponse = {
  items: siteservicev1_Navigation[] | undefined;
  total: number | undefined;
};

// 导航
export type siteservicev1_Navigation = {
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  id?: number;
  isActive?: boolean;
  items: siteservicev1_NavigationItem[] | undefined;
  locale?: string;
  location?: siteservicev1_Navigation_Location;
  name?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 导航位置枚举
export type siteservicev1_Navigation_Location =
  | 'FOOTER'
  // 核心位置（必需）
  | 'HEADER'
  | 'MOBILE'
  | 'NAVIGATION_LOCATION_UNSPECIFIED'
  | 'OFFCANVAS'
  | 'SIDEBAR'
  // 可选扩展（按需添加）
  | 'TOP_BAR';
// 导航项目项
export type siteservicev1_NavigationItem = {
  children: siteservicev1_NavigationItem[] | undefined;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  description?: string;
  icon?: string;
  id?: number;
  isInvalid?: boolean;
  isOpenNewTab?: boolean;
  linkType?: siteservicev1_NavigationItem_LinkType;
  navigationId?: number;
  objectId?: number;
  parentId?: number;
  requiredPermission?: string;
  sortOrder?: number;
  title?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
  url?: string;
};

// 链接类型
export type siteservicev1_NavigationItem_LinkType =
  | 'LINK_TYPE_CATEGORY'
  | 'LINK_TYPE_CUSTOM'
  | 'LINK_TYPE_EXTERNAL'
  | 'LINK_TYPE_PAGE'
  | 'LINK_TYPE_POST'
  | 'LINK_TYPE_UNSPECIFIED';
// 请求 - 导航数据
export type siteservicev1_GetNavigationRequest = {
  id?: number;
  name?: string;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建导航
export type siteservicev1_CreateNavigationRequest = {
  data: siteservicev1_Navigation | undefined;
};

// 请求 - 更新导航
export type siteservicev1_UpdateNavigationRequest = {
  allowMissing?: boolean;
  data: siteservicev1_Navigation | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除导航
export type siteservicev1_DeleteNavigationRequest = {
  id?: number;
};

// 页面服务
export interface PageService {
  // 获取页面列表
  List(
    request: pagination_PagingRequest,
  ): Promise<contentservicev1_ListPageResponse>;
  // 获取页面数据
  Get(
    request: contentservicev1_GetPageRequest,
  ): Promise<contentservicev1_Page>;
  // 创建页面
  Create(
    request: contentservicev1_CreatePageRequest,
  ): Promise<contentservicev1_Page>;
  // 更新页面
  Update(
    request: contentservicev1_UpdatePageRequest,
  ): Promise<contentservicev1_Page>;
  // 删除页面
  Delete(
    request: contentservicev1_DeletePageRequest,
  ): Promise<wellKnownEmpty>;
  // 获取翻译数据
  GetTranslation(
    request: contentservicev1_GetPageRequest,
  ): Promise<contentservicev1_PageTranslation>;
}

export function createPageServiceClient(
  transport: ClientTransport,
): PageService {
  return {
    List(request) {
      const path = `app/v1/pages`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PageService',
        method: 'List',
      }) as Promise<contentservicev1_ListPageResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/pages/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.slug) {
        queryParams.push(
          `slug=${encodeURIComponent(request.slug.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PageService',
        method: 'Get',
      }) as Promise<contentservicev1_Page>;
    },
    Create(request) {
      const path = `app/v1/pages`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'PageService',
        method: 'Create',
      }) as Promise<contentservicev1_Page>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/pages/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'PageService',
        method: 'Update',
      }) as Promise<contentservicev1_Page>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/pages/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'PageService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
    GetTranslation(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/pages/${request.id}/translation`;
      const body = null;
      const queryParams: string[] = [];
      if (request.slug) {
        queryParams.push(
          `slug=${encodeURIComponent(request.slug.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PageService',
        method: 'GetTranslation',
      }) as Promise<contentservicev1_PageTranslation>;
    },
  };
}
// 回应 - 页面列表
export type contentservicev1_ListPageResponse = {
  items: contentservicev1_Page[] | undefined;
  total: number | undefined;
};

// 页面
export type contentservicev1_Page = {
  authorId?: number;
  authorName?: string;
  availableLanguages: string[] | undefined;
  children: contentservicev1_Page[] | undefined;
  contentModelId?: number;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  customFields: { [key: string]: string } | undefined;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  depth?: number;
  disallowComment?: boolean;
  editorType?: contentservicev1_EditorType;
  id?: number;
  isCustomTemplate?: boolean;
  parentId?: number;
  path?: string;
  redirectUrl?: string;
  // 页面区块（随页面整体读写，Create/Update 时整体替换，对齐 content_model.fields 惯例）
  sections: contentservicev1_Section[] | undefined;
  showInNavigation?: boolean;
  slug?: string;
  sortOrder?: number;
  status?: contentservicev1_Page_PageStatus;
  template?: string;
  thumbnail?: string;
  translations: contentservicev1_PageTranslation[] | undefined;
  type?: contentservicev1_Page_PageType;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 页面状态
export type contentservicev1_Page_PageStatus =
  | 'PAGE_STATUS_ARCHIVED'
  | 'PAGE_STATUS_DRAFT'
  | 'PAGE_STATUS_PUBLISHED'
  | 'PAGE_STATUS_UNSPECIFIED';
// 页面类型（用于特殊页面处理）
export type contentservicev1_Page_PageType =
  | 'PAGE_TYPE_CUSTOM'
  | 'PAGE_TYPE_DEFAULT'
  | 'PAGE_TYPE_ERROR_404'
  | 'PAGE_TYPE_ERROR_500'
  | 'PAGE_TYPE_HOME'
  | 'PAGE_TYPE_UNSPECIFIED';
// 页面翻译
export type contentservicev1_PageTranslation = {
  coverImage?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  fullPath?: string;
  id?: number;
  languageCode?: string;
  pageId?: number;
  seo?: contentservicev1_SeoMeta;
  slug?: string;
  title?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 页面区块（语言无关的结构，内容随语言变化在翻译表中）。
// 作为 Page 的嵌套子部件随页面整体读写，无独立服务入口。
export type contentservicev1_Section = {
  availableLanguages: string[] | undefined;
  config: { [key: string]: string } | undefined;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  id?: number;
  name?: string;
  pageId?: number;
  sortOrder?: number;
  translations: contentservicev1_SectionTranslation[] | undefined;
  type?: contentservicev1_SectionType;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 区块类型
export type contentservicev1_SectionType =
  | 'SECTION_TYPE_BUTTON'
  | 'SECTION_TYPE_CAROUSEL'
  | 'SECTION_TYPE_CODE'
  | 'SECTION_TYPE_CUSTOM'
  | 'SECTION_TYPE_DIVIDER'
  | 'SECTION_TYPE_FORM'
  | 'SECTION_TYPE_GALLERY'
  | 'SECTION_TYPE_HTML'
  | 'SECTION_TYPE_IMAGE'
  | 'SECTION_TYPE_MARKDOWN'
  | 'SECTION_TYPE_RICH_TEXT'
  | 'SECTION_TYPE_SPACER'
  | 'SECTION_TYPE_TITLE'
  | 'SECTION_TYPE_UNSPECIFIED'
  | 'SECTION_TYPE_VIDEO';
// 区块翻译（语言相关的内容）
export type contentservicev1_SectionTranslation = {
  content: { [key: string]: string } | undefined;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  id?: number;
  languageCode?: string;
  sectionId?: number;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 请求 - 页面数据
export type contentservicev1_GetPageRequest = {
  id?: number;
  locale?: string;
  slug?: string;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建页面
export type contentservicev1_CreatePageRequest = {
  data: contentservicev1_Page | undefined;
};

// 请求 - 更新页面
export type contentservicev1_UpdatePageRequest = {
  allowMissing?: boolean;
  data: contentservicev1_Page | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除页面
export type contentservicev1_DeletePageRequest = {
  id?: number;
};

// 帖子服务
export interface PostService {
  // 获取帖子列表
  List(
    request: pagination_PagingRequest,
  ): Promise<contentservicev1_ListPostResponse>;
  // 全文搜索帖子（前台，基于 OpenSearch）
  // 
  // 仅返回 PUBLISHED 状态内容；tenant_id 由服务端从登录用户上下文注入，
  // 客户端无法指定或绕过。故此端点不进鉴权白名单——必须登录后方可搜索。
  SearchPosts(
    request: contentservicev1_SearchPostsRequest,
  ): Promise<contentservicev1_SearchPostsResponse>;
  // 获取帖子数据
  Get(
    request: contentservicev1_GetPostRequest,
  ): Promise<contentservicev1_Post>;
  // 创建帖子
  Create(
    request: contentservicev1_CreatePostRequest,
  ): Promise<contentservicev1_Post>;
  // 更新帖子
  Update(
    request: contentservicev1_UpdatePostRequest,
  ): Promise<contentservicev1_Post>;
  // 删除帖子
  Delete(
    request: contentservicev1_DeletePostRequest,
  ): Promise<wellKnownEmpty>;
  // 获取翻译数据
  GetTranslation(
    request: contentservicev1_GetPostRequest,
  ): Promise<contentservicev1_PostTranslation>;
}

export function createPostServiceClient(
  transport: ClientTransport,
): PostService {
  return {
    List(request) {
      const path = `app/v1/posts`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PostService',
        method: 'List',
      }) as Promise<contentservicev1_ListPostResponse>;
    },
    SearchPosts(request) {
      const path = `app/v1/posts/search`;
      const body = null;
      const queryParams: string[] = [];
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.language) {
        queryParams.push(
          `language=${encodeURIComponent(request.language.toString())}`,
        );
      }
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PostService',
        method: 'SearchPosts',
      }) as Promise<contentservicev1_SearchPostsResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/posts/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PostService',
        method: 'Get',
      }) as Promise<contentservicev1_Post>;
    },
    Create(request) {
      const path = `app/v1/posts`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'PostService',
        method: 'Create',
      }) as Promise<contentservicev1_Post>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/posts/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'PostService',
        method: 'Update',
      }) as Promise<contentservicev1_Post>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/posts/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'PostService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
    GetTranslation(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/posts/${request.id}/translation`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'PostService',
        method: 'GetTranslation',
      }) as Promise<contentservicev1_PostTranslation>;
    },
  };
}
// 请求 - 帖子搜索
// 
// tenant_id 不在此消息中——由服务端从调用方租户上下文（viewer）注入，
// 客户端无法指定或绕过。status 固定为 PUBLISHED（前台仅检索已发布内容）。
export type contentservicev1_SearchPostsRequest = {
  // 语言代码（必填，仅返回该语言的翻译命中）
  language: string | undefined;
  // 页码（0-based）
  page: number | undefined;
  // 每页条数（服务端封顶 50）
  pageSize: number | undefined;
  // 搜索查询词
  query: string | undefined;
};

// 回应 - 帖子搜索
// 
// 仅返回最小字段集：post_id / language / title。
// 不含 content / tenant_id / status——这些字段不向前台暴露。
export type contentservicev1_SearchPostsResponse = {
  items: contentservicev1_SearchPostHit[] | undefined;
  total: number | undefined;
};

// 搜索命中条目（最小字段集）
export type contentservicev1_SearchPostHit = {
  // 语言代码
  language: string | undefined;
  // 帖子 ID（用于后续 GetPost 详情查询）
  postId: number | undefined;
  // 标题（来自翻译，用于搜索结果展示）
  title: string | undefined;
};

// 请求 - 帖子数据
export type contentservicev1_GetPostRequest = {
  code?: string;
  id?: number;
  locale?: string;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建帖子
export type contentservicev1_CreatePostRequest = {
  data: contentservicev1_Post | undefined;
};

// 请求 - 更新帖子
export type contentservicev1_UpdatePostRequest = {
  allowMissing?: boolean;
  data: contentservicev1_Post | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除帖子
export type contentservicev1_DeletePostRequest = {
  id?: number;
};

// 站点服务（公开端点）
// 仅 GetSiteByDomain 对外可用：按请求 Host 解析的域名返回当前站点的渲染必需配置。
// List/Create/Update/Delete 在 app（公开站点）服务上禁用，写操作应经由 admin 服务。
export interface SiteService {
  // 获取站点列表（公开端点禁用）
  List(
    request: pagination_PagingRequest,
  ): Promise<siteservicev1_ListSiteResponse>;
  // 按域名获取当前站点配置（公开：domain 由 BFF 按 Host 填入，调用方不可指定）
  GetSiteByDomain(
    request: siteservicev1_GetSiteByDomainRequest,
  ): Promise<siteservicev1_Site>;
  // 创建站点（公开端点禁用）
  Create(
    request: siteservicev1_CreateSiteRequest,
  ): Promise<siteservicev1_Site>;
  // 更新站点（公开端点禁用）
  Update(
    request: siteservicev1_UpdateSiteRequest,
  ): Promise<siteservicev1_Site>;
  // 删除站点（公开端点禁用）
  Delete(
    request: siteservicev1_DeleteSiteRequest,
  ): Promise<wellKnownEmpty>;
}

export function createSiteServiceClient(
  transport: ClientTransport,
): SiteService {
  return {
    List(request) {
      const path = `app/v1/sites`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'SiteService',
        method: 'List',
      }) as Promise<siteservicev1_ListSiteResponse>;
    },
    GetSiteByDomain(request) {
      const path = `app/v1/site`;
      const body = null;
      const queryParams: string[] = [];
      if (request.domain) {
        queryParams.push(
          `domain=${encodeURIComponent(request.domain.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'SiteService',
        method: 'GetSiteByDomain',
      }) as Promise<siteservicev1_Site>;
    },
    Create(request) {
      const path = `app/v1/sites`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'SiteService',
        method: 'Create',
      }) as Promise<siteservicev1_Site>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/sites/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'SiteService',
        method: 'Update',
      }) as Promise<siteservicev1_Site>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/sites/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'SiteService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// 回应 - 站点列表
export type siteservicev1_ListSiteResponse = {
  items: siteservicev1_Site[] | undefined;
  total: number | undefined;
};

// 站点
export type siteservicev1_Site = {
  alternateDomains: string[] | undefined;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  defaultLocale?: string;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  domain?: string;
  id?: number;
  isDefault?: boolean;
  name?: string;
  slug?: string;
  status?: siteservicev1_Site_Status;
  template?: string;
  tenantId?: number;
  theme?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 站点状态
export type siteservicev1_Site_Status =
  | 'SITE_STATUS_ACTIVE'
  | 'SITE_STATUS_INACTIVE'
  | 'SITE_STATUS_MAINTENANCE'
  | 'SITE_STATUS_UNSPECIFIED';
// 请求 - 按域名获取站点数据
export type siteservicev1_GetSiteByDomainRequest = {
  domain: string | undefined;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建站点
export type siteservicev1_CreateSiteRequest = {
  data: siteservicev1_Site | undefined;
};

// 请求 - 更新站点
export type siteservicev1_UpdateSiteRequest = {
  allowMissing?: boolean;
  data: siteservicev1_Site | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除站点
export type siteservicev1_DeleteSiteRequest = {
  id?: number;
};

// 标签服务
export interface TagService {
  // 获取标签列表
  List(
    request: pagination_PagingRequest,
  ): Promise<contentservicev1_ListTagResponse>;
  // 获取标签数据
  Get(
    request: contentservicev1_GetTagRequest,
  ): Promise<contentservicev1_Tag>;
  // 创建标签
  Create(
    request: contentservicev1_CreateTagRequest,
  ): Promise<contentservicev1_Tag>;
  // 更新标签
  Update(
    request: contentservicev1_UpdateTagRequest,
  ): Promise<contentservicev1_Tag>;
  // 删除标签
  Delete(
    request: contentservicev1_DeleteTagRequest,
  ): Promise<wellKnownEmpty>;
  // 获取翻译数据
  GetTranslation(
    request: contentservicev1_GetTagRequest,
  ): Promise<contentservicev1_TagTranslation>;
}

export function createTagServiceClient(
  transport: ClientTransport,
): TagService {
  return {
    List(request) {
      const path = `app/v1/tags`;
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(
          `page=${encodeURIComponent(request.page.toString())}`,
        );
      }
      if (request.pageSize) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.offset) {
        queryParams.push(
          `offset=${encodeURIComponent(request.offset.toString())}`,
        );
      }
      if (request.limit) {
        queryParams.push(
          `limit=${encodeURIComponent(request.limit.toString())}`,
        );
      }
      if (request.token) {
        queryParams.push(
          `token=${encodeURIComponent(request.token.toString())}`,
        );
      }
      if (request.noPaging) {
        queryParams.push(
          `noPaging=${encodeURIComponent(request.noPaging.toString())}`,
        );
      }
      if (request.query) {
        queryParams.push(
          `query=${encodeURIComponent(request.query.toString())}`,
        );
      }
      if (request.filter) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.filterExpr?.type) {
        queryParams.push(
          `filterExpr.type=${encodeURIComponent(request.filterExpr.type.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.field) {
        queryParams.push(
          `filterExpr.conditions.field=${encodeURIComponent(request.filterExpr.conditions.field.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.op) {
        queryParams.push(
          `filterExpr.conditions.op=${encodeURIComponent(request.filterExpr.conditions.op.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.value) {
        queryParams.push(
          `filterExpr.conditions.value=${encodeURIComponent(request.filterExpr.conditions.value.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonValue) {
        queryParams.push(
          `filterExpr.conditions.jsonValue=${encodeURIComponent(request.filterExpr.conditions.jsonValue.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.values) {
        request.filterExpr.conditions.values.forEach((x) => {
          queryParams.push(
            `filterExpr.conditions.values=${encodeURIComponent(x.toString())}`,
          );
        });
      }
      if (request.filterExpr?.conditions?.datePart) {
        queryParams.push(
          `filterExpr.conditions.datePart=${encodeURIComponent(request.filterExpr.conditions.datePart.toString())}`,
        );
      }
      if (request.filterExpr?.conditions?.jsonPath) {
        queryParams.push(
          `filterExpr.conditions.jsonPath=${encodeURIComponent(request.filterExpr.conditions.jsonPath.toString())}`,
        );
      }
      if (request.orderBy) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.sorting?.field) {
        queryParams.push(
          `sorting.field=${encodeURIComponent(request.sorting.field.toString())}`,
        );
      }
      if (request.sorting?.direction) {
        queryParams.push(
          `sorting.direction=${encodeURIComponent(request.sorting.direction.toString())}`,
        );
      }
      if (request.fieldMask) {
        queryParams.push(
          `fieldMask=${encodeURIComponent(request.fieldMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'TagService',
        method: 'List',
      }) as Promise<contentservicev1_ListTagResponse>;
    },
    Get(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/tags/${request.id}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'TagService',
        method: 'Get',
      }) as Promise<contentservicev1_Tag>;
    },
    Create(request) {
      const path = `app/v1/tags`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'TagService',
        method: 'Create',
      }) as Promise<contentservicev1_Tag>;
    },
    Update(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/tags/${request.id}`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'TagService',
        method: 'Update',
      }) as Promise<contentservicev1_Tag>;
    },
    Delete(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/tags/${request.id}`;
      const body = null;
      return transport.unary(path, 'DELETE', body, {
        service: 'TagService',
        method: 'Delete',
      }) as Promise<wellKnownEmpty>;
    },
    GetTranslation(request) {
      if (request.id === undefined || request.id === null) {
        throw new Error('missing required field request.id');
      }
      const path = `app/v1/tags/${request.id}/translation`;
      const body = null;
      const queryParams: string[] = [];
      if (request.code) {
        queryParams.push(
          `code=${encodeURIComponent(request.code.toString())}`,
        );
      }
      if (request.locale) {
        queryParams.push(
          `locale=${encodeURIComponent(request.locale.toString())}`,
        );
      }
      if (request.viewMask) {
        queryParams.push(
          `viewMask=${encodeURIComponent(request.viewMask.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary(uri, 'GET', body, {
        service: 'TagService',
        method: 'GetTranslation',
      }) as Promise<contentservicev1_TagTranslation>;
    },
  };
}
// 回应 - 标签列表
export type contentservicev1_ListTagResponse = {
  items: contentservicev1_Tag[] | undefined;
  total: number | undefined;
};

// 标签
export type contentservicev1_Tag = {
  availableLanguages: string[] | undefined;
  code?: string;
  color?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  group?: string;
  icon?: string;
  id?: number;
  isFeatured?: boolean;
  postCount?: number;
  sortOrder?: number;
  status?: contentservicev1_Tag_TagStatus;
  translations: contentservicev1_TagTranslation[] | undefined;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 标签状态
export type contentservicev1_Tag_TagStatus =
  | 'TAG_STATUS_ACTIVE'
  | 'TAG_STATUS_ARCHIVED'
  | 'TAG_STATUS_HIDDEN'
  | 'TAG_STATUS_UNSPECIFIED';
// 标签翻译
export type contentservicev1_TagTranslation = {
  coverImage?: string;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  description?: string;
  fullPath?: string;
  id?: number;
  languageCode?: string;
  name?: string;
  seo?: contentservicev1_SeoMeta;
  slug?: string;
  tagId?: number;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
};

// 请求 - 标签数据
export type contentservicev1_GetTagRequest = {
  code?: string;
  id?: number;
  locale?: string;
  viewMask?: wellKnownFieldMask;
};

// 请求 - 创建标签
export type contentservicev1_CreateTagRequest = {
  data: contentservicev1_Tag | undefined;
};

// 请求 - 更新标签
export type contentservicev1_UpdateTagRequest = {
  allowMissing?: boolean;
  data: contentservicev1_Tag | undefined;
  id: number | undefined;
  updateMask: undefined | wellKnownFieldMask;
};

// 请求 - 删除标签
export type contentservicev1_DeleteTagRequest = {
  id?: number;
};

// 用户个人资料服务
export interface UserProfileService {
  // 获取用户资料
  GetUser(
    request: wellKnownEmpty,
  ): Promise<identityservicev1_User>;
  // 更新用户资料
  UpdateUser(
    request: identityservicev1_UpdateUserRequest,
  ): Promise<wellKnownEmpty>;
  // 修改用户密码
  ChangePassword(
    request: identityservicev1_ChangePasswordRequest,
  ): Promise<wellKnownEmpty>;
  // 绑定手机号码/邮箱
  BindContact(
    request: identityservicev1_BindContactRequest,
  ): Promise<wellKnownEmpty>;
  // 验证手机号码/邮箱
  VerifyContact(
    request: identityservicev1_VerifyContactRequest,
  ): Promise<wellKnownEmpty>;
}

export function createUserProfileServiceClient(
  transport: ClientTransport,
): UserProfileService {
  return {
    GetUser(_request) {
      const path = `app/v1/me`;
      const body = null;
      return transport.unary(path, 'GET', body, {
        service: 'UserProfileService',
        method: 'GetUser',
      }) as Promise<identityservicev1_User>;
    },
    UpdateUser(request) {
      const path = `app/v1/me`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'PUT', body, {
        service: 'UserProfileService',
        method: 'UpdateUser',
      }) as Promise<wellKnownEmpty>;
    },
    ChangePassword(request) {
      const path = `app/v1/me/password`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'UserProfileService',
        method: 'ChangePassword',
      }) as Promise<wellKnownEmpty>;
    },
    BindContact(request) {
      const path = `app/v1/me/contact`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'UserProfileService',
        method: 'BindContact',
      }) as Promise<wellKnownEmpty>;
    },
    VerifyContact(request) {
      const path = `app/v1/me/contact/verify`;
      const body = JSON.stringify(request);
      return transport.unary(path, 'POST', body, {
        service: 'UserProfileService',
        method: 'VerifyContact',
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// 用户
export type identityservicev1_User = {
  address?: string;
  avatar?: string;
  commentCount?: number;
  createdAt?: wellKnownTimestamp;
  createdBy?: number;
  deletedAt?: wellKnownTimestamp;
  deletedBy?: number;
  description?: string;
  email?: string;
  followers?: number;
  following?: number;
  gender?: identityservicev1_User_Gender;
  id?: number;
  lastLoginAt?: wellKnownTimestamp;
  lastLoginIp?: string;
  likeCount?: number;
  lockedUntil?: wellKnownTimestamp;
  mobile?: string;
  nickname?: string;
  orgUnitId?: number;
  orgUnitIds: number[] | undefined;
  orgUnitName?: string;
  orgUnitNames: string[] | undefined;
  positionId?: number;
  positionIds: number[] | undefined;
  positionName?: string;
  positionNames: string[] | undefined;
  postCount?: number;
  realname?: string;
  region?: string;
  remark?: string;
  roleId?: number;
  roleIds: number[] | undefined;
  roleNames: string[] | undefined;
  roles: string[] | undefined;
  status?: identityservicev1_User_Status;
  telephone?: string;
  tenantId?: number;
  tenantName?: string;
  updatedAt?: wellKnownTimestamp;
  updatedBy?: number;
  username?: string;
};

// 用户性别
export type identityservicev1_User_Gender =
  | 'FEMALE'
  | 'MALE'
  | 'SECRET';
// 用户状态
export type identityservicev1_User_Status =
  | 'CLOSED'
  | 'DISABLED'
  | 'EXPIRED'
  | 'LOCKED'
  | 'NORMAL'
  | 'PENDING';
// 更新用户 - 请求
export type identityservicev1_UpdateUserRequest = {
  allowMissing?: boolean;
  //
  // Behaviors: REQUIRED
  data: identityservicev1_User | undefined;
  id: number | undefined;
  password?: string;
  updateMask: undefined | wellKnownFieldMask;
};

// 修改用户密码（需要验证旧密码） - 请求
export type identityservicev1_ChangePasswordRequest = {
  newPassword: string | undefined;
  oldPassword: string | undefined;
};

export type identityservicev1_BindContactRequest = {
  email?: identityservicev1_BindEmailRequest;
  phone?: identityservicev1_BindPhoneRequest;
};

export type identityservicev1_BindPhoneRequest = {
  code: string | undefined;
  phone: string | undefined;
};

export type identityservicev1_BindEmailRequest = {
  email: string | undefined;
  verificationCode?: string;
};

export type identityservicev1_VerifyContactRequest = {
  email?: identityservicev1_EmailVerification;
  phone?: identityservicev1_PhoneVerification;
  // 可选：服务端生成的验证码会话 id（用于多步骤或回调验证）
  verificationId?: string;
};

// 手机验证
export type identityservicev1_PhoneVerification = {
  //
  // Behaviors: REQUIRED
  code: string | undefined;
  phone: string | undefined;
};

// 邮箱验证
export type identityservicev1_EmailVerification = {
  //
  // Behaviors: REQUIRED
  code: string | undefined;
  email: string | undefined;
};

export class ApiClient {
  private _authenticationService?: AuthenticationService;
  private _categoryService?: CategoryService;
  private _commentService?: CommentService;
  private _fileTransferService?: FileTransferService;
  private _interactionService?: InteractionService;
  private _navigationService?: NavigationService;
  private _pageService?: PageService;
  private _postService?: PostService;
  private _siteService?: SiteService;
  private _tagService?: TagService;
  private readonly _transport: ClientTransport;
  private _userProfileService?: UserProfileService;

  constructor(transport: ClientTransport) {
    this._transport = transport;
  }

  get authenticationService(): AuthenticationService {
    return this._authenticationService ??= createAuthenticationServiceClient(this._transport);
  }

  get categoryService(): CategoryService {
    return this._categoryService ??= createCategoryServiceClient(this._transport);
  }

  get commentService(): CommentService {
    return this._commentService ??= createCommentServiceClient(this._transport);
  }

  get fileTransferService(): FileTransferService {
    return this._fileTransferService ??= createFileTransferServiceClient(this._transport);
  }

  get interactionService(): InteractionService {
    return this._interactionService ??= createInteractionServiceClient(this._transport);
  }

  get navigationService(): NavigationService {
    return this._navigationService ??= createNavigationServiceClient(this._transport);
  }

  get pageService(): PageService {
    return this._pageService ??= createPageServiceClient(this._transport);
  }

  get postService(): PostService {
    return this._postService ??= createPostServiceClient(this._transport);
  }

  get siteService(): SiteService {
    return this._siteService ??= createSiteServiceClient(this._transport);
  }

  get tagService(): TagService {
    return this._tagService ??= createTagServiceClient(this._transport);
  }

  get userProfileService(): UserProfileService {
    return this._userProfileService ??= createUserProfileServiceClient(this._transport);
  }
}

export function createApiClient(transport: ClientTransport): ApiClient {
  return new ApiClient(transport);
}


// @@protoc_insertion_point(typescript-http-eof)
