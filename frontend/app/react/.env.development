# 本地环境变量配置（不要提交到版本控制）
# dev server 端口号（由 scripts/dev.mjs 探测可用端口；被占用时自动顺延）
PORT=5001

# API 接口地址
NEXT_PUBLIC_API_BASE_URL=http://localhost:6700

# 是否启用 Mock 数据
NEXT_PUBLIC_ENABLE_MOCK=false

# 应用标题
NEXT_PUBLIC_APP_TITLE='GoWind Content Hub'

# 应用描述
NEXT_PUBLIC_APP_DESCRIPTION='A modern Content Hub built with Next.js'

# 默认语言
NEXT_PUBLIC_DEFAULT_LOCALE=zh-CN

# Token 配置
NEXT_PUBLIC_TOKEN_KEY=access_token
NEXT_PUBLIC_REFRESH_TOKEN_KEY=refresh_token
NEXT_PUBLIC_AES_KEY=f51d66a73d8a0927
